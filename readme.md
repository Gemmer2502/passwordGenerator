A password generator to train myself.
You can choose the length of the password and the characters used. Show the password in the console and write it in a file called "password.txt".

INSTALLATION:
-"git clone git@github.com:Gemmer2502/passwordGenerator.git"
-"cd passwordGenerator"
-"python -m venv .venv"
-"source .venv/Scripts/activate" (this is for windows, replace "Scripts" by "bin" if you're on Mac or Linux)
-"pip install -r requirements.txt"

USAGE:
-"python main.py"